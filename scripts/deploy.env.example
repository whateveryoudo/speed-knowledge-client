# 复制为同目录 deploy.env 后按需修改（deploy.env 勿提交）
# cp scripts/deploy.env.example scripts/deploy.env

ECS_HOST=8.137.203.142
ECS_USER=root

# nginx 静态资源目录（与 ECS docker-compose nginx volume 一致）
ECS_WEB_DIR=/var/www/speed-knowledge

# rsync 额外参数（可选）
# RSYNC_EXTRA=--dry-run
